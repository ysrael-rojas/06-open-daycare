{
  "$schema": "https://opencode.ai/config.json",
  "lsp": true,
  "references": {
    "db-schema": {
      "path": "../07-DB-Schema",
      "description": "Schema de base de datos, tablas, columnas y referencias a crear, esto no esta implementado en la base de datos, es solo un esquema de referencia para el desarrollo."
    }
  },
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["npx", "-y", "@playwright/mcp@latest"],
      "enabled": true
    }
  }
}
